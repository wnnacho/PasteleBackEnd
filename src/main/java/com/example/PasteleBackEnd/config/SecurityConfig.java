package com.example.PasteleBackEnd.config;

/**
 * Configuración de seguridad previa (deshabilitada).
 * Esta clase se mantiene sólo como referencia histórica.
 * La seguridad activa está en el paquete `security` con JWT.
 */
public class SecurityConfig {
    // Intencionalmente sin @Configuration ni beans.
}
